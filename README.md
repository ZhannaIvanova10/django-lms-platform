# Django LMS Project

Проект системы управления обучением (LMS) на Django с Django REST Framework.

## ✅ Выполненные задания

### 📋 Задание 1: SerializerMethodField для подсчета уроков
- Реализовано поле `lessons_count` в сериализаторе курса
- Метод `get_lessons_count` подсчитывает количество уроков в курсе
- **Тестирование:** http://127.0.0.1:8000/api/courses/

### 📋 Задание 2: Модель Payment (платежи)
- Создана модель `Payment` с полями:
  - `user` (ForeignKey к User)
  - `course` (ForeignKey к Course, опционально)
  - `lesson` (ForeignKey к Lesson, опционально)
  - `amount` (сумма платежа)
  - `payment_method` (способ оплаты: cash/transfer)
  - `payment_date` (дата платежа)
- Созданы тестовые данные (10 платежей)
- **Тестирование:** http://127.0.0.1:8000/api/payments/

### 📋 Задание 3: Вложенные уроки в курсе
- Добавлено поле `lessons` в `CourseSerializer`
- Использован `LessonSerializer` для вложенного представления
- Каждый курс отображает список своих уроков
- **Тестирование:** http://127.0.0.1:8000/api/courses/

### 📋 Задание 4: Фильтрация платежей
- Установлен и настроен `django-filter`
- Реализована фильтрация по:
  - Курсу (`?course=1`)
  - Уроку (`?lesson=1`)
  - Способу оплаты (`?payment_method=cash`)
- Реализована сортировка:
  - По сумме (`?ordering=amount` или `?ordering=-amount`)
  - По дате (`?ordering=payment_date` или `?ordering=-payment_date`)
- **Тестирование:**
  - Все платежи: http://127.0.0.1:8000/api/payments/
  - Только cash: http://127.0.0.1:8000/api/payments/?payment_method=cash
  - Сортировка по сумме: http://127.0.0.1:8000/api/payments/?ordering=amount

## 🚀 Запуск проекта

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Примените миграции:
```bash
python manage.py migrate
```

3. Создайте суперпользователя:
```bash
python manage.py createsuperuser
```

4. Запустите сервер:
```bash
python manage.py runserver
```

5. Создайте тестовые данные (опционально):
```bash
python create_test_data.py
```

## 📊 API Endpoints

- `GET /api/courses/` - Список курсов с уроками и их количеством
- `GET /api/lessons/` - Список уроков
- `GET /api/payments/` - Список платежей с фильтрацией и сортировкой
- `GET /api/users/` - Список пользователей
- `GET /admin/` - Админка Django

## 🛠️ Технологии

- Django 6.0.1
- Django REST Framework 3.16.1
- Django Filter 25.2
- SQLite (база данных)

## 📁 Структура проекта

```
django_lms_project/
├── config/              # Настройки проекта
├── users/               # Приложение пользователей
│   ├── models.py       # Модели User, Payment
│   ├── serializers.py  # Сериализаторы
│   ├── views.py        # ViewSets
│   └── admin.py        # Админка
├── materials/           # Приложение материалов
│   ├── models.py       # Модели Course, Lesson
│   ├── serializers.py  # Сериализаторы
│   └── views.py        # ViewSets
├── payments_fixture.json # Тестовые данные платежей
├── create_test_data.py  # Скрипт создания тестовых данных
└── requirements.txt     # Зависимости
```

## 📈 Тестовые данные

Проект содержит тестовые данные:
- 2 пользователя (админ и студент)
- 4 курса
- 9 уроков (по 2-3 на каждый курс)
- 10 платежей

## 🔧 Админка

Админка Django доступна по адресу: http://127.0.0.1:8000/admin/

Доступные модели:
- Пользователи
- Курсы
- Уроки
- Платежи

## 🎯 Дополнительные функции

- Кастомная модель User с дополнительными полями
- Вложенные сериализаторы для связанных данных
- Фильтрация и сортировка через query parameters
- Готовые тестовые данные

## 📄 Лицензия

Учебный проект
