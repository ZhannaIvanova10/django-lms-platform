# 🎓 ФИНАЛЬНЫЙ ОТЧЕТ: Django LMS Platform

## ✅ ВЫПОЛНЕННЫЕ ЗАДАНИЯ

### 📝 ЗАДАНИЕ 1: JWT-аутентификация
✅ Реализована регистрация пользователей через /api/users/
✅ JWT авторизация через /api/token/ и /api/token/refresh/
✅ Все эндпоинты защищены токенами
✅ Кастомный юзер с дополнительными полями

### 👥 ЗАДАНИЕ 2: Группа модераторов
✅ Создана группа "moderators" с правами:
   - Просмотр всех объектов
   - Редактирование всех объектов
   - НЕ может создавать объекты
   - НЕ может удалять объекты
✅ Настроены пермишены: IsModerator, IsOwner

### 🔐 ЗАДАНИЕ 3: Права доступа для объектов
✅ Пользователи видят только свои курсы/уроки
✅ Владельцы имеют полный доступ к своим объектам
✅ Модераторы имеют доступ ко всем объектам (кроме create/delete)
✅ Автоматическая привязка владельца при создании

### 🎯 ДОПОЛНИТЕЛЬНОЕ ЗАДАНИЕ
✅ Профиль пользователя /api/profile/
✅ CRUD операции для профиля
✅ Только владелец может редактировать свой профиль
## 🔐 КЛЮЧЕВЫЕ НАСТРОЙКИ:

### JWT Configuration:
```python
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',
    ]
}
Permissions System:
IsModerator: проверка группы модераторов

IsOwner: проверка владельца объекта

Комбинирование: IsOwner | IsModerator, ~IsModerator

ViewSet Rights Management:
python
def get_permissions(self):
    if self.action == 'create':
        self.permission_classes = [IsAuthenticated, ~IsModerator]
    elif self.action in ['update', 'partial_update']:
        self.permission_classes = [IsAuthenticated, IsOwner | IsModerator]
    elif self.action == 'destroy':
        self.permission_classes = [IsAuthenticated, IsOwner, ~IsModerator]
    else:
        self.permission_classes = [IsAuthenticated]
    return [permission() for permission in self.permission_classes]
🧪 РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ
✅ ТЕСТ 1: Защита эндпоинтов
GET /api/courses/ без токена → 401 Unauthorized ✓

GET /api/courses/ с токеном → 200 OK с данными ✓

✅ ТЕСТ 2: Регистрация и авторизация
POST /api/users/ без токена → 201 Created ✓

POST /api/token/ с credentials → 200 OK с токенами ✓

✅ ТЕСТ 3: Права доступа
Обычный пользователь → только свои объекты ✓

Модератор → все объекты (view/edit), не может create/delete ✓

Владелец → полный доступ к своим объектам ✓

✅ ТЕСТ 4: Автоматическая привязка
При создании курса/урока автоматически назначается владелец ✓

Все существующие объекты имеют владельцев ✓

✅ ТЕСТ 5: Профиль пользователя
GET /api/profile/ с токеном → 200 OK с данными профиля ✓

Редактирование профиля доступно только владельцу ✓

📊 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ API
Для Windows PowerShell:
powershell
# 1. Регистрация пользователя
curl -X POST http://127.0.0.1:8000/api/users/ `
  -H "Content-Type: application/json" `
  -d '{"email":"test@example.com","password":"Password123","password2":"Password123","first_name":"Тест"}'

# 2. Получение JWT токена
curl -X POST http://127.0.0.1:8000/api/token/ `
  -H "Content-Type: application/json" `
  -d '{"email":"test@example.com","password":"Password123"}'

# 3. Доступ к защищенному эндпоинту
curl -X GET http://127.0.0.1:8000/api/courses/ `
  -H "Authorization: Bearer ваш_jwt_токен"
Для Windows (через py):
bash
# 1. Запуск сервера
py manage.py runserver

# 2. Создание суперпользователя
py manage.py createsuperuser

# 3. Запуск тестов
py test_auth_final.py

# 4. Применение миграций
py manage.py migrate
🚀 КАК ЗАПУСТИТЬ ПРОЕКТ
Установка и настройка:
bash
# 1. Клонировать репозиторий
git clone https://github.com/ZhannaIvanova10/django-lms-platform.git
cd django-lms-platform

# 2. Установить зависимости
pip install -r requirements.txt

# 3. Применить миграции
py manage.py migrate

# 4. Создать суперпользователя (опционально)
py manage.py createsuperuser
Запуск проекта:
bash
# Запустить сервер
py manage.py runserver

# Сервер будет доступен по адресу:
# http://127.0.0.1:8000
🎯 ВЫВОД
Все задания выполнены успешно!

✅ JWT-аутентификация реализована и работает корректно
✅ Система прав доступа настроена с разграничением ролей
✅ Модераторы имеют ограниченные права (view/edit без create/delete)
✅ Обычные пользователи работают только со своими объектами
✅ Дополнительное задание (профиль пользователя) выполнено
✅ Все эндпоинты протестированы и работают как ожидается

Проект готов к проверке! 🏆


🔗 ССЫЛКИ
GitHub репозиторий: https://github.com/ZhannaIvanova10/django-lms-platform

Работающий сервер: http://127.0.0.1:8000

Админка: http://127.0.0.1:8000/admin/

API документация: В README.md проекта

👨‍💻 АВТОР
Жанна Иванова
Студент курса Django разработки
Проект выполнен в рамках учебного задания

Дата завершения: 25 января 2026 года
